{
  "name": "naivi-counter-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "naivi counter demo — dual-platform: `nv web` (pure Vite fallback) and U4 wasm (`nv wasm --release` + trunk in packages/naivi-wasm), plus `nv desktop` (native).",
  "scripts": {
    "dev": "nv web",
    "build:wasm": "nv wasm --release"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@naivi/runtime": "workspace:*",
    "vue": "^3.6.0-rc.1"
  },
  "devDependencies": {
    "@naivi/cli": "workspace:*",
    "@types/node": "^22.12.0",
    "@vitejs/plugin-vue": "^6.0.8",
    "typescript": "^5.7.0",
    "vite": "^8.2.0"
  }
}
